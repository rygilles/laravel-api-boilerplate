<?php

namespace App\Http\Requests;

use App\Models\{{ modelClassName }};

class Store{{ modelClassName }}Request extends ApiRequest
{
    /**
     * Get the validation rules that apply to the request.
     *
     * @return array
     */
    public function rules()
    {
        return $this->filterWithModelConfiguration({{ modelClassName }}::class, {{ modelClassName }}::getStoreRules());
    }
}
